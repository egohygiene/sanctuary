# VLC

Purpose

Shared VLC configuration for the Sanctuary workstation.

Only intentional overrides should be stored here.

Do not commit a generated `vlcrc` containing every VLC preference.

Most settings should remain upstream defaults.

