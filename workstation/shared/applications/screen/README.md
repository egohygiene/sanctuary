# GNU Screen

Purpose

Portable GNU Screen configuration for the Sanctuary workstation.

Only intentional overrides are stored here.

Terminal multiplexing is primarily handled by tmux, but Screen remains
useful on many remote systems where tmux is unavailable.

