# ------------------------------------------------------------------------------
# FUSE
# ------------------------------------------------------------------------------
#
# Originally introduced for VMware shared-folder support through
# open-vm-tools. The configuration itself is generic and ensures the
# FUSE kernel module is loaded at boot.
#
# ------------------------------------------------------------------------------

fuse
