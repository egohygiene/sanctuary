# Purpose

Custom GRUB themes and bootloader assets.

Installation:

```shell
sudo cp -r bigsur /boot/grub/themes/
```

Edit:

```shell
GRUB_THEME="/boot/grub/themes/bigsur/theme.txt"
```

Then:

```shell
sudo update-grub
```
