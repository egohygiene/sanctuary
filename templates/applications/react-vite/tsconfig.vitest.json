{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",

  "compilerOptions": {
    // 🧪 Enable Vitest global test functions
    "types": ["vitest/globals"]
  },

  "include": [
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "tests"
  ],

  "exclude": [
    "node_modules",
    "dist",
    "generated",
    ".cache"
  ]
}
