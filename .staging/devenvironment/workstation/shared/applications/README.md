# Shared Application Configuration

This directory contains reusable or example configuration for applications
that are not tied to a specific operating system.

These configurations are intended to serve as:

- reference implementations
- reusable workstation defaults
- starting points for future customization

Machine-specific state and generated files should not be committed here.

