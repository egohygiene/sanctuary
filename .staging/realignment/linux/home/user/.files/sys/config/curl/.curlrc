# -*-mode:curlrc-*- vim:ft=dosini

# curl Configuration File (.curlrc)
# -------------------------------------------------------------------
# This file contains configuration settings for curl, a command-line
# utility for transferring data with URLs. It allows customization of
# various aspects of curl's behavior including proxy settings, user
# agent, and more.
#
# The settings defined in this file will be applied globally to all
# curl operations initiated by the user.
#
# Usage:
# Place this file in your home directory or refer to it using the
# --config option when running curl.
#
# Example:
#   curl --config=/path/to/.curlrc [URL]
#
# Note: Modify the settings in this file according to your specific
# download requirements and network configuration.
#
# Reference Links:
# - curl Manual: https://curl.se/docs/manual.html
# - curl Command-line Options: https://curl.se/docs/manpage.html
# - curlrc Configuration: https://curl.se/docs/manual.html#THE-CURLRC-FILE
# -------------------------------------------------------------------

# Enable/disable host-prefixed file names.
add_hostdir = on

# Prompt for a password for each connection established.
ask_password = off

# If this option is given, curl will send Basic HTTP authentication information for all requests.
#auth_no_challenge = off

# Enable/disable going to background.
background = off

# Enable/disable saving pre-converted files with the suffix '.orig'.
#backup_converted = on

# Use up to number backups for a file.
#backups = 0

# Specify the maximum number of redirects to follow.
max_redirects = 20

# Turn off DNS caching.
#dns_cache = on

# Set the DNS timeout.
dns_timeout = 10

# Set the connect timeout.
connect_timeout = 900

# Set the read timeout.
#read_timeout = 900

# Follow only relative links.
#relative_only = off

# Set the default page title.
#default_page = string

# Set the client certificate file name.
#certificate = file

# Specify the type of the client certificate.
#certificate_type = string

# Ignore case when matching files and directories.
#ignore_case = off

# Ignore Content-Length header.
#ignore_length = off

# Ignore robots.txt and <meta name=robots content=nofollow>.
#robots = off

# Ignore symbolic links.
#retr_symlinks = off

# Retry even when the connection was refused.
retry_connrefused = on

# Save the headers sent by the HTTP server to the file, preceding the actual contents, with an empty line as the separator.
#save_headers = off

# Choose the secure protocol to be used.
ssl_version = auto

# Print the headers sent by HTTP servers and responses sent by FTP servers.
verbose = off

# Specify the user agent string.
user_agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/58.0.3029.110 Safari/537.3"



























# cURL configuration options.
# https://curl.haxx.se/docs/manpage.html

# Limit the time (in seconds) the connection is allowed to take.
connect-timeout = 60

# Follow HTTP redirects.
location

# Display progress as a simple progress bar.
progress-bar

# Show error messages.
show-error

# When following a redirect, automatically set the previous URL as referer.
referer = ";auto"

# Use the server-provided timestamp, if any.
remote-time

# Retry the transfer if a transient error occurs.
retry = 5

# Consider connections refused as transient errors.
retry-connrefused

# Define the user agent identification header.
# user-agent = "Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:72.0) Gecko/20100101 Firefox/72.0"
