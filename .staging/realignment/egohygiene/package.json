{
  "name": "egohygiene",
  "private": true,
  "version": "0.1.0",
  "description": "🌿 Schema-driven spiritual design system and app ecosystem.",
  "workspaces": [
    "apps/*",
    "packages/*",
    "cli",
    "tools/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.10.13"
  }
}
