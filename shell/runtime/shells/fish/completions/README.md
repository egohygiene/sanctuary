# This directory is intentionally empty.
#
# Add Fish shell completion files here as the runtime grows.
# Each .fish file placed here is automatically added to fish_complete_path
# by runtime.fish at startup.
